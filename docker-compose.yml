version: "3.9"

services:
  web:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./static/contents:/app/static/contents
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
